# 代码库新人上手说明（当前仓库状态）

## 1. 现状总览

当前仓库仍处于**初始化骨架阶段**，还没有真实业务代码。

- 已有文件：
  - `.gitkeep`（占位文件）
- 暂无内容：
  - 源码目录（如 `src/` / `app/`）
  - 测试目录（如 `tests/`）
  - 工程化配置（CI、Lint、Formatter）
  - 项目说明（README）

> 结论：现在不是“读代码”阶段，而是“搭建规范与工程底座”阶段。

## 2. 新人需要先了解的重点

由于还没有业务模块，建议新人优先掌握以下内容：

1. **项目目标与边界**：这个仓库要解决什么问题，输入/输出是什么。
2. **技术栈决策**：语言、框架、包管理器、运行方式。
3. **代码规范**：命名、目录结构、提交规范、分支策略。
4. **质量保障**：单测策略、静态检查、CI 门禁。

## 3. 建议尽快补齐的目录与文件

建议先建立一个“可持续迭代”的最小结构：

- `README.md`：
  - 项目简介、安装方式、运行方式、常见命令。
- `src/`（或语言对应目录）：
  - 放置核心业务代码。
- `tests/`：
  - 单元测试 / 集成测试。
- `docs/`：
  - 架构说明、术语表、开发流程。
- CI + Lint + Format 配置：
  - 保证提交代码的可读性与稳定性。

## 4. 新人后续学习建议（按顺序）

### 第 0 步：先跑通最小闭环

- 能在本地执行：安装依赖 → 启动项目 → 运行测试（即便只有示例测试）。

### 第 1 步：读 README 与 docs

- 重点看：
  - 项目要做什么
  - 本地如何开发
  - 代码如何提交与评审

### 第 2 步：从“最小功能”开始贡献

- 先做低风险任务：
  - 文档补充
  - 测试补齐
  - 小型工具函数

### 第 3 步：建立工程习惯

- 每次提交前固定执行：
  - 格式化
  - 静态检查
  - 测试

### 第 4 步：再进入架构层面

- 当仓库出现多个模块后，再重点学习：
  - 模块边界
  - 依赖方向
  - 关键调用链

## 5. 给维护者的下一步建议

为了让新成员尽快进入状态，建议维护者优先补齐：

1. `README.md`（必需）
2. 一个可运行的 `src/` 最小示例（必需）
3. 至少 1 个可执行测试（强烈建议）
4. 最小 CI 校验（强烈建议）

---

如果你希望，我可以在下一步直接产出：

- `README.md` 模板（含安装/运行/测试章节）
- 适配你技术栈的目录脚手架建议
- 新人一周 onboarding checklist
